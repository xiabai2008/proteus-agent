# 修复待办清单（Backlog）

> 建档：2026-09-21　　维护方式：每次会话发现的可修复点追加于此，统一编号，不改原文。
> 编号规则：`R-<序号>`（Review 发现，本清单自有）；已有的 `F-E2E-<n>` 沿用原编号不重编。
> 状态：`待修` / `已修` / `已评审收口` / `观察`。
> **每一条都附 file:line 锚点与本轮实测证据**——不写"大概在某个文件"。

---

## 修复进度（2026-09-21 本轮）

| 编号 | 状态 | 落地 |
|---|---|---|
| R-1 | **已修** | `eccd212` — MCP server 加服务端 `default_mode`（构造期校验），省略 mode 时回落 |
| R-3 | **已修** | `mode.skills` 按 `Skill.category` 机制性过滤；`reflect.py` 提取 category（此前该字段无消费方） |
| R-4 | **已修** | 删除 `mcp.py` 的死代码 `self._agents` |
| R-5 | **已修** | DSH 指南第八节第 3/4/5 条重写为已修状态，附"历史修正"注记指向实测记录第九节 |
| R-6 | **已修** | `b97aed6` — `proteus-ctf` approval 由 `ask` 改为 `never`（用户决策） |
| R-7 | **已修** | README 四处 219 全部改为 236（本轮实测数） |
| R-8 | **已修** | `cli.py` 的 docstring 与 `prog` 改用 `python -m penagent` |
| R-9 | **已修** | adapters 统一进 `build_center`；`agents` 改走与内核同款的两层过滤链 |
| R-11 | **已修** | `cd3edec` — 容器不可用的错误消息补可操作修复指引（不引入静默降级） |
| R-14 | **已修** | `0b0b3c2` — `build_center` 加 `discover_mcp` 开关；DSH preset 已开启 |
| R-2 | **已标注** | 四个 YAML 的 `max_cost_usd` 加"预留字段、当前不生效"注释 |
| R-10 / R-12 / R-13 | 观察 | 保留说明（见各条目）：R-13 已在 DSH 指南第八节补充"两套闸"说明 |

**测试**：236 passed（本轮净增 8 例：R-11 ×1、R-14 ×2、R-3 ×5；R-9 另补 3 例）。
**仍未解决**：R-11 的**基础设施**部分（本机 Docker 守护进程）——内核侧已给指引，
但 Docker 恢复前 `sandbox=docker` 模式的高危工具仍会被拒（这是正确的 fail-closed）。

**2026-09-22 追加**：R-22（宿主桥审计未按 preset 过滤，新发现）、R-23（裁决层边界，观察）
见下文。同日本轮另修 `http_raw` 回灌截断导致的 13 步重试环、spool 路径归一引起的重复入链
（过程与实测见 `docs/DSH插件化与内核旁路治理.md`，未单开 R 编号）。

---

## 一、结构性缺口（最高优先级）

### R-1 DSH 主线下「不传 mode」= 无任何模式保护（**已修** `eccd212`）

> 状态（2026-09-22 复核）：**已修**——MCP server 在构造期解析服务端 `default_mode`，
> 省略 `mode` 时回落到该模式而非 `None`。下面的现象描述保留为**修复前**的实测记录，
> 不是当前行为（原文不动，状态以本行为准）。

**位置**：`penagent/mcp.py` 的 `_agent(mode_id="")` → `mode=None` 分支

**现象**（本轮实测，2026-09-21）：

```
=== 无模式(DSH 模型没传 mode) ===
  mode      = None
  sandbox   = None          <- 沙箱策略完全缺失
  max_steps = 12            <- 而非模式的 40/60
  memory.ns = default       <- 而非模式分区（记忆/技能落错分区）
  工具数    = 20
  sqlmap_auto: dangerous=True -> 无沙箱裁决，直跑宿主
  nuclei_scan: dangerous=True -> 无沙箱裁决，直跑宿主
  fscan_scan : dangerous=True -> 无沙箱裁决，直跑宿主
```

**为什么严重**：DSH 会话里模型调 `mcp__proteus__pentest_run` 时，`mode` 是**可选参数**
（`mcp.py` 的 `SERVER_TOOLS` 描述里写了建议值，但属提示词级约束）。模型一旦不传：

1. **沙箱裁决完全消失**——危险 CLI 工具（sqlmap / nuclei / fscan）**直跑宿主**
2. 记忆/技能落到 `default` 分区，模式间隔离失效
3. 步数预算退回 12，判定器退回默认 `evidence_chain`（CTF 任务永远拿不到 flag 判定）
4. 违反 AGENTS.md 硬规则 1（"模式约束必须机制性生效，禁止靠模型自觉"）——这里是靠模型自觉传参

**实测到的荒谬对称性**（同一台机器，Docker 守护进程不可用）：

| 路径 | sqlmap/nuclei/fscan 结果 |
|---|---|
| 不传 mode | **直跑宿主**（无隔离） |
| 传 pentest-standard（sandbox=docker） | **被拒绝**（容器不可用） |

即：**为了让工具能用，用户/模型会倾向于不传 mode，从而失去全部模式保护**。

**建议修法**（待定，需设计）：给 `PentestMCPServer` 加默认模式（启动参数或从 DSH preset 传入），
未显式指定时回落到默认模式，而非 `None`；并把 mode 选择从"模型可选参数"提升为
"服务端配置"（与 `--targets` / `--authorize` 同级）。

---

## 二、静默失效 / 声明了但不生效

### R-2 `budget.max_cost_usd` 声明但未生效（已标注，待决策）

- **位置**：`penagent/modes.py:184-190` 注释已诚实写明"预留字段"；`modes/base.yaml:16` 写 `max_cost_usd: 5.0`；`modes/ctf-web.yaml:16`、`modes/ctf-crypto.yaml:14` 写 `1.0`
- **现状**：字段被解析、被校验、被展示，但主循环**从不消费**——网关不回 token 用量
- **风险**：读 YAML 的人会误以为有成本硬约束
- **建议**：要么接网关用量统计真实计量，要么在三个 YAML 里同步加注释标注"当前不生效"

### R-3 `ModeProfile.skills` 字段无消费方（待修或废弃）

- **位置**：`penagent/modes.py:236`（定义）、`:424`（解析）；消费点仅 `web/server.py:70`（前端展示）与 `tests/test_modes.py:93`（断言）
- **AGENTS.md 第 4 节**称其语义为"技能检索过滤"，**实际检索不过滤该字段**——`penagent/agent.py:344` 的 `find_skills` 只按 `target_fingerprint` 检索，实际的模式隔离靠 `memory_namespace`
- **建议**：二选一——让 `_rank_skills` 真正按 `mode.skills` 过滤；或明确废弃该字段（文档说明其展示用途），消除"写了但没用"的静默失效

### R-4 `penagent/mcp.py:80` 死代码 `self._agents`（待清理）

- **位置**：`penagent/mcp.py:80` `self._agents: dict[str, PenAgent] = {}`
- **证据**：全文件 `grep _agents` 仅此一处——声明后从未读写
- **语义**：原设计意图是缓存 agent 实例（避免每次 `pentest_run` 重建），未实现
- **建议**：要么实现缓存（MCP 效率可再提升），要么删除

---

## 三、文档与实现漂移

### R-5 `docs/DSH宿主接入指南.md` 第八节已过期，方向是「低估自身安全性」（待修）

- **位置**：`docs/DSH宿主接入指南.md:263` 起的「八、边界与已知限制」
- **过期断言**（行锚点已按当前文件逐行复核，2026-09-21 修正）：
  - `:274-281`（第 3 条）"内核的 MCP 底层工具入口**目前直连注册表、未过 Policy**……那份 policy 是死代码"
  - `:308-309`（第 5 条）"该前提只在 `pentest_run` 路径成立；`mcp__proteus__<底层工具>` 路径不成立"
  - `:291`（第 4 条）"失败一律报成成功：`isError` **恒为 `false`**"
  - 另注：原稿误写 `:266-271` / `:290-291` / `:324`（`:324` 超出文件末尾），已由提交后审查发现并更正
- **实际状态**：上述两条均已在 commit `02557bd` 修复。本轮逐条核对当前代码：
  - `penagent/mcp.py` `__init__` 已有 `self.registry.gate = PolicyGate(self.policy)`
  - `penagent/tools.py` `execute()` 在工具体之前调 `self.gate.check(spec, args)`
  - `penagent/mcp.py` 底层工具已 `is_error=not tool_result.ok`；`pentest_run` 已 `is_error=result.outcome != "success"`
- **复测证据已存在**：`docs/DSH宿主实测记录.md:462` 第九节（R2 打 `127.0.0.2` → `isError=true` + `duration_ms: 0`，证明拒绝发生在工具体之前）
- **根因**：改指南的 commit `90e5149` 在修复 commit `02557bd` **之后**，却未同步第八节
- **风险**：未来的会话/用户按过期指南判断，以为底层工具路径不设防
- **建议**：重写第八节第 3、4、5 条，指向 `DSH宿主实测记录.md` 第九节

### R-6 `proteus-ctf` 的 approval 档位三处不一致（待定值后修）

- **代码**（权威）：`dsh/proteus.cordis.patch.yml:52-54` → `proteus-ctf: sandbox: danger-full-access` / `approval: ask`
- **文档**：`docs/DSH宿主接入指南.md:78` 表格写 `never`；`:259` 写 `approval: never`；`:287-288` 写 `proteus-ctf（approval: never + danger-full-access）`
- **补丁自身注释**：`dsh/proteus.cordis.patch.yml:53` 附近写"danger-full-access + **never** 仍意味着无人值守最宽姿态"
- **冲突**：代码是 `ask`（会逐步打断），文档与注释是 `never`（不打断）——行为语义差异大
- **阻塞**：需先确定设计意图（`ask` 还是 `never`），再统一三处

### R-7 README 测试数四处写 219，实测 224（待修）

- **位置**：`README.md:7`（徽章）、`:92`（架构图层）、`:152`（目录导航）、`:207`（状态段）
- **证据**：本轮 `run_tests` 实测 **224 passed**（28.27s）
- **建议**：改为 224，并在 CI 徽章旁注明数据日期

### R-8 `cli.py` docstring 用 `pentest` 命令，实际入口是 `python -m penagent`（待修）

- **位置**：`penagent/cli.py:4-6`（docstring 示例写 `pentest run ...` / `pentest reflect ...`）
- **证据**：仓库无 `pyproject.toml` / `setup.py` / `setup.cfg`，无 console_script 安装；本轮 `which pentest` → 未找到；`python -m penagent --help` → 正常
- **影响**：低（README 的快速开始写的是正确形式），但会误导直接读源码注释的人
- **建议**：docstring 改用 `python -m penagent`，或补 `pyproject.toml` 让 `pentest` 真正可用

---

## 四、可用性 / 一致性

### R-9 `agents` 子命令列出的工具与 `--mode` 实际挂载不一致（待修）

- **位置**：`penagent/cli.py:119-127` `cmd_agents` 用 `ToolRegistry() + register_builtins + load_external_tools + adapters` 构建
- **问题**：不走 `build_center()`，故该命令**看不到** CTF 工具（`rsactf_attack` / `python_solve` / `codec_*`）与沙箱档位；而 `run --mode ctf-crypto` 实际挂载的是 5 个 CTF 工具
- **证据**：本轮实测 `python -m penagent agents` → 24 工具（全侦察类）；`_agent(mode_id='ctf-crypto')` → 5 工具
- **影响**："我能用哪些工具"的答案因入口而异
- **建议**：`cmd_agents` 增加 `--mode` 参数并改走 `build_center()`；或分别列出"内核工具"与"按模式挂载的工具"

### R-10 `pentest-standard.yaml` 双命名体系维护脆性（观察）

- **位置**：`modes/pentest-standard.yaml:12-13`（注释自陈）`auto_approve` 同时含契约键（`httpx`/`subfinder`/`dnsx`）与内核注册名（`http_probe`/`port_scan`/…）
- **影响**：一次工具改名要同步改 YAML 两处 + 测试，长期是坑
- **建议**：待 `F-E2E-1` 类问题的根因（契约名与注册名不统一）彻底解决后，统一为单一命名

### R-11 Docker 守护进程闪断导致隔离工具不可用（F-E2E-4，观察）

- **证据**（本轮实测）：传 `pentest-standard` 时 `sqlmap_auto`/`nuclei_scan`/`fscan_scan` 全部 `allowed=False`，原因 `sandbox=docker 但容器不可用（docker 守护进程不可用: failed to connect to the docker API）`
- **影响**：**本机当前无法执行任何高危 CLI 工具**（只要它们的模式是 sandbox=docker）
- **这是 R-1 的直接诱因**：用户为绕开此限制会不传 mode，反而失去全部保护
- **建议**：属基础设施问题，但内核侧应给出**可操作的提示**（当前只是 `allowed=False` + 一句原因）；或支持"降级为 local 但要求显式确认"的档位

**2026-09-21 复核（Docker 恢复后）**：基础设施已恢复——`docker version` → **29.5.2**，
`docker run --rm hello-world` → 退出码 0；`sandbox=docker` 档对危险工具由
`allowed=False` 变为 **`allowed=True, isolated=True`**（sqlmap/nuclei/fscan 实测）。

**但复核揪出一个此前被掩盖的真实缺陷**：`DockerRunner.wrap()` 把宿主路径
**同时用作** `-v` 的容器侧与 `-w`，而容器只认 Linux 路径。实测：

| 写法 | 结果 |
|---|---|
| `-v <宿主>:/work -w /work` | returncode **0**，输出 `container-ok` ✓ |
| `-v <宿主>:<宿主> -w <宿主>`（原写法） | returncode **125**，`the working directory 'D:\...' is invalid` ✗ |

Linux 上 `os.getcwd()` 本就是合法容器路径，缺陷不显形；**Windows 上隔离执行全线失败**。
此前被掩盖是因为 Docker 不可用时 `decide()` 直接拒绝、根本走不到 `wrap()`。

**已修**：宿主路径只用于 `-v` 宿主侧，容器侧固定映射 `CONTAINER_WORKDIR = "/work"`，
`-w` 也用容器内路径。回归用例
`tests/test_sandbox.py::test_docker_container_execution_actually_works`（**真跑容器**，
修复前必然失败）。

**仍未闭环（新记录为 R-15）**：见下条。

### R-14 外部 MCP server（RayScan / Chameleon / seckb）在主路径从未被连接（待修）

- **位置**：`penagent/registry.py:221` 定义 `discover_mcp()`；**生产路径无调用点**——仅 `examples/mcp_e2e_probe.py:29`（探针）与 `tests/test_registry.py:122/137/149/165/191`（测试桩，共 5 处）调用
- **证据**（本轮 grep）：`penagent/cli.py` 与 `penagent/mcp.py` 都只调 `build_center()`，而 `build_center()` 只做 `load_mcp_servers()`（**登记声明**），不调 `discover_mcp()`（**实际连接**）
- **后果**：`penagent/mcp_servers.json` 声明的 3 个外部 server（RayScan 3 工具 / Chameleon 12 工具 / seckb 知识库 4 工具）**在实际运行中全部不可用**
- **旁证**：`docs/DSH宿主实测记录.md` 实测注册 **24** 个工具 = 内置 4 + 外部 CLI 16 + 服务端 4，**恰好不含任何外部 MCP 工具**
- **文档漂移**：`README.md` 称"知识检索（seckb 知识库）与外部工具（RayScan / Chameleon）按 `penagent/mcp_servers.json` 声明**自动接入**"——实为"自动登记声明，需手动 discover"
- **设计取舍**：注释写明是为避免启动时阻塞（"discover_mcp 前不连接任何外部服务"），取舍合理，但**缺一个开关**让用户能选择接入
- **建议**：给 CLI/MCP 加 `--discover-mcp`（或配置项）显式连接；修正 README 措辞

### R-15 容器内跑不了外置工具（专用镜像缺口）（**已闭环** 2026-09-23）

- **发现时间**：2026-09-21（R-11 复核时）
- **现象**：路径缺陷修复后，容器内**能跑 python**，但跑不了宿主的外置工具
  （sqlmap / nuclei / fscan / RsaCtfTool 等）：
  - 默认镜像 `python:3.12-slim` 内没有这些二进制；
  - `ctf_tools.json` 的 `{python}` 展开为**宿主**解释器路径
    （如 `<PY312>\python.exe`），容器内不存在。
- **证据**：实测 `-v <宿主>:/work -w /work python:3.12-slim python -c ...`
  → returncode 0（容器内有 python）；同一写法把命令换成宿主 python 绝对路径则失败。
- **影响**：`sandbox=docker` 模式下外部 CLI 工具的隔离执行**实际不可用**——
  只有内置 function 工具与镜像自带的 python 能真跑。
- **建议**：建专用镜像（预装所需工具）或用 requirements 注入；
  `PENTEST_DOCKER_IMAGE` 环境变量已预留镜像切换（`penagent/sandbox.py`）。
  `docs/沙箱降级评估.md` 已把此项列为"待 Docker 稳定后补测"，本次复核确认
  缺口真实存在。

**2026-09-21 实施（CTF 侧闭环）**：

- **专用镜像** `docker/Dockerfile.sandbox` → `proteus-sandbox:latest`（261MB），
  内装 RsaCtfTool。构建方式与 `PENTEST_DOCKER_IMAGE` 配置见 `.env.example`。
  构建过程中踩到两个环境约束，都写进了 Dockerfile 注释：
  - **github.com 直连不可达**（宿主与容器实测均超时）→ 默认走 GitHub 代理
    （`ghproxy.net`，实测两侧均可达），可用 `--build-arg RSACTFTOOL_BASE` 覆盖；
  - **RsaCtfTool 不在 PyPI**（发行名 `rsactftool` 查询无结果），只能从 GitHub 装，
    故钉住 commit；且**不显式列 `pycryptodome`**——RsaCtfTool 自带该依赖（3.10.4），
    再装最新版会版本冲突（实测 build 失败）。
- **命令容器化** `containerize_command()`（`penagent/sandbox.py`）：把命令里的
  宿主解释器路径重写为容器内 `python`——`ctf_tools.json` 的 `{python}` 展开为
  `sys.executable`，容器内不存在。只重写解释器路径，其它宿主路径不无脑替换
  （那会把错误藏起来）。
- **实测（决定性）**：容器内 `python -m RsaCtfTool --help` → 0；用容器内的
  RsaCtfTool **真解出** `simple-rsa` 的 flag（输出含 `flag{rsa_close_primes_pwned}`）。

**2026-09-21 第二轮（渗透侧扩展）**：

- **`containerize_command()` 通用化**：原先只重写解释器路径；现在也把宿主工具
  二进制路径（`.../sqlmap.exe`）映射为容器内**工具名**（靠 PATH 解析）。
- **补齐 `load_external_tools` 的字段解析**——这是本轮挖出的更基础缺陷：它原只
  解析 8 个字段，**漏了 `network` / `sandbox` / `arg_flags`**（与
  `registry.load_cli_config` 的 schema 不同步）。后果是 `external_tools.json`
  即使写了 `network: true` 也**不生效**，工具在容器内一律 `--network none` 断网
  ——装了也扫不到目标。已对齐。
- `external_tools.json` 的 **16 个工具如实声明 `network: true`**（目标都是远程主机）。
- Dockerfile 增加 **sqlmap**（零配置依赖：不需模板/字典/PoC 文件）。
- **接线验证（回应"built != wired"）**：经 `registry.execute` 的**生产路径**执行
  sqlmap → 返回 `1.10.9#pip`。此前只有临时合成 ToolSpec 的验证，没有生产消费方。
- **端到端实证**：容器内 sqlmap **真扫**了宿主的 Juice Shop
  （`http://host.docker.internal:3000/rest/products/search?q=test`）——输出含
  `testing connection to the target URL` / `GET parameter 'q' appears to be
  dynamic` / `testing 'AND boolean-based blind ...`，即容器内工具**真的在干活**
  （不只是 `--version`）。这也验证了容器网络语义：容器内 `127.0.0.1` 是容器
  自己，访问宿主须用 `host.docker.internal` 且工具要 `network=true` 走 bridge。

**2026-09-21 第三轮（Go 工具扩容：fscan / naabu / dalfox）**：

- **镜像扩容**（`docker/Dockerfile.sandbox`）：三个 Go 二进制装进
  `/usr/local/bin`（靠 PATH 解析，配合 `containerize_command` 的工具名重写）。
  选型标准与上同：**零配置依赖**，不需要模板/字典。镜像 **285MB → 342MB**。
- **供应链完整性**：版本号 + **官方 sha256 双钉**（`ARG` 声明），下载后校验，
  不过即**构建失败**——这三个二进制会在隔离容器里以 root 执行，被代理换成
  别的构建等于让沙箱执行来路不明的代码。校验和取自各 release 官方
  `checksums.txt` / `.sha256` 资产。
- **代理实测（选型证据）**：github.com 直连 `code=000`（不可达）；
  `ghproxy.net` 吞吐约 **43KB/s 且把 11.8MB 的 naabu 资产截断**（实得 5.2MB，
  sha256 不符）；`gh-proxy.com` 约 **5.8MB/s 且三资产完整**。故 Go 工具段默认
  走 `gh-proxy.com`（`--build-arg GO_TOOLS_BASE=` 可覆盖；rsactftool 段保持
  `ghproxy.net` 原样——其资产小，已验证可用）。
- **不引入 curl/unzip**：用镜像自带的 python 下载/校验/解包（三种形态：
  裸 ELF / zip / tar.gz）。
- **实测（决定性，全部经生产路径 `registry.execute` 在容器内真扫宿主）**：
  - **fscan**：命令取 `external_tools.json` 的 `fscan_scan` **原样**
    （`-h host.docker.internal -p 21,22,80,443,8000,8080,18080 -nobr -np`）
    → 命中 8080 的 DVWA：`title:Login :: Damn Vulnerable Web Application
    (DVWA) v1.10`，`扫描任务完成`；
  - **naabu**：`-host <ip> -p <port> -sr -silent` → 输出 `<ip>:<port>`；
  - **dalfox**：`url --url <target>` 打自带反射靶页 → `found reflected 1 params`
    并产出 POC（`Payload: <svg onload=alert(1) ...>`）。
- **回归用例 4 条**（`tests/test_sandbox.py`）：镜像自检 + 三工具真扫。
  真扫用例自带**反射靶页 fixture**（宿主临时端口，容器经
  `host.docker.internal` 回来），不依赖 Juice Shop；环境不可达时 skip。
  用例只按输出断言：**dalfox 找到 POC 时退出码是 1**（扫描器"有发现"约定，
  实测 有 POC→1 / 无发现→0），`result.ok` 在此不表示失败。

**第 4 轮（2026-09-23）：需要模板 / 字典的那些也闭环了。**

原判"需先把数据文件打进镜像"是**错的**——正确做法是**不打进镜像**：这些数据体积
大、更新频繁、且逐机不同，塞进镜像既臃肿又要跟着更新。改成

1. **宿主目录只读挂载**（`penagent/sandbox.py` 的 `host_data_mounts`）：
   - `${PENTEST_TOOLS}` → `/opt/host-tools`（字典 / PoC）
   - `<用户主目录>/nuclei-templates` → `/root/nuclei-templates`
     （nuclei 的**默认**模板目录，故 spec 不必加 `-t`，宿主侧行为不变）
2. **容器化时按根重写**（`containerize_command(command, data_root=...)`）：
   spec 里的 `${PENTEST_TOOLS}/config/dictionaries/10k-most-common.txt` →
   `/opt/host-tools/config/dictionaries/10k-most-common.txt`。
   **顺序很关键**：工具二进制先按"取工具名"重写（`.../tools/nuclei.exe` → `nuclei`），
   否则那个 Windows `.exe` 会被当成数据文件映射成 `/opt/host-tools/tools/*.exe`——
   在 Linux 容器里必然跑不起来（已单独钉用例）。只重写**声明的数据根之下**的路径，
   根外路径一律不动——通用替换会把"数据真缺"这类错误藏起来。
3. **镜像新增四个工具**（`docker/Dockerfile.sandbox` 第四轮段）：nuclei 3.11.1 /
   ffuf 2.3.0 / gobuster 3.8.2（Go 二进制，**版本 + 官方 sha256 双钉**，含 arm64）
   + pocsuite3（pip；不带 `-r` 时用它自带的 PoC 目录，零数据即可跑）。

**已验证（不需要 Docker 的那部分）**：把 Dockerfile 里的下载脚本**原样抽出来**在宿主
上跑了一遍——三个资产的 sha256 与钉的值逐位一致（nuclei 143MB / ffuf 10.5MB /
gobuster 9.6MB），说明代理可达、版本与资产名正确、两种压缩包（zip / **裸** tar.gz）
的解包逻辑正确。
**尚未执行**：容器内真跑（本机 Docker 处于暂停状态）。镜像重建：

```bash
docker build -f docker/Dockerfile.sandbox -t proteus-sandbox:latest docker/
```

恢复 Docker 后 `python -m pytest tests/test_sandbox.py -q` 即拿到真扫证据（新增 5 条：
ffuf / gobuster 命中隐藏路径、nuclei 命中工作目录模板、模板目录可见、宿主字典在
挂载点可读）。

**仍未覆盖（诚实界定）**：

- `poxiao_scan` / `ruoyi_scan` 依赖**宿主项目代码**，无法容器化（保持宿主直跑）。

### R-16 `pentest-standard` 的 network_egress=false 与渗透工具出网需求矛盾（**已决策并实施 2026-09-21**）

- **发现时间**：2026-09-21（R-15 第二轮）
- **现象**：本轮给 `external_tools.json` 的 16 个工具如实声明了 `network: true`
  （目标都是远程主机，必须出网）。但 `pentest-standard` 的
  `scope.network_egress: false` 会**拒绝所有声明出网的工具**——即该模式下
  16 个渗透工具全部不可用。
- **证据**：`penagent/agent.py` 的 `Policy.check` —
  `if not self.mode.scope.network_egress and getattr(spec, "network", False):`
  → 拒绝并给出"模式关闭网络出口"；`penagent/sandbox.py` 的
  `SandboxPolicy.decide` 有同款裁决（两层冗余）。
- **为什么此前没暴露**：`external_tools.json` 的工具**从未声明过 `network`**
  （上一轮才补的字段解析），这条闸门从未触发——**补了声明才让它显形**。
- **决策（2026-09-21，用户拍板选①）**：`pentest-standard` 改
  `network_egress: true`——承认渗透测试本质必须触达目标，"禁止出网"的
  渗透模式语义自相矛盾；目标边界由已有的 `target_allowlist` 硬校验独立
  承担（硬规则 3，任何模式下越界目标一律拒绝），出网开关不重复设闸。
- **实施**：`modes/pentest-standard.yaml` 的 scope 行改为
  `{target_allowlist: required, network_egress: true}` 并附决策注释；
  `modes/base.yaml` 保持 `false` 不变（安全缺省，由各模式显式覆盖）；
  `Policy.check` / `SandboxPolicy.decide` 两层裁决**代码未动**——机制
  （egress=false 拒绝出网工具）保留，供其他模式按需使用。
- **遗留注意**：出网后目标可达面变宽，但 `target_allowlist` 仍是硬边界
  （`Policy.authorize` 白名单校验先于一切工具执行），安全姿态不受削弱。

### R-17 `dalfox_xss` 的调用形态与 dalfox v3.2.0 不兼容（**已修 2026-09-21**）

- **发现时间**：2026-09-21（R-15 第三轮，容器内真跑 dalfox 时）
- **现象**：`external_tools.json` 的 `dalfox_xss` 以**位置参数**传目标
  （`dalfox url <target> --silence`，`positional: true`）。dalfox v3.2.0 要求
  `dalfox url --url <URL>`，位置参数直接报
  `error: the following required arguments were not provided: --url <URL>`。
- **证据**：**宿主与容器都复现**——`${PENTEST_TOOLS}/tools/dalfox.exe url
  "http://127.0.0.1:3000/..." --silence` 同样报错。即该工具在本轮之前
  **在哪都跑不起来**（不是容器化引入的问题）。
- **修复**：参数键 `target` → `url`、去掉 `positional: true`，渲染为
  `dalfox url --url <target> --silence`（`ToolRegistry._cli_args` 的
  `--{key}` 规则，实测渲染为 `['--url', '<target>']`）。
- **回归**：`tests/test_sandbox.py::test_sandbox_dalfox_real_scan_finds_reflection`
  用注册表 spec 的真实命令形态扫自带靶页，断言 `found reflected` 与 `Payload:`。

### R-18 naabu 内置解析器读不到容器特殊名 `host.docker.internal`（已缓解 2026-09-21）

- **发现时间**：2026-09-21（R-15 第三轮）
- **现象**：`naabu -host host.docker.internal ...` 报
  `FTL Could not run enumeration: no valid ipv4 or ipv6 targets were found`；
  加 `-v` 后可见 `WRN no IP addresses found for host: host.docker.internal`。
- **根因**：容器内 `/etc/hosts` **并无** `host.docker.internal` 条目
  （实测只有 localhost 与本机名），该名字由 Docker DNS
  （`/etc/resolv.conf` → `192.168.65.7`）解析；naabu 用自带解析器，
  **不读 `/etc/hosts`**。且该路径解析**不稳定**：同一命令连跑 3 次，
  实测约一半直接报错（DNS 抖动）。
- **影响面**：只影响"工具自带解析器 + Docker 特殊名"的组合。公网/内网域名
  （走正常 DNS）与 IP 目标不受影响——**生产路径的常见目标形态没问题**。
- **缓解**：
  1. `external_tools.json` 的 `naabu_scan` 追加 `-sr`（system-resolver 兜底，
     实测对 127.0.0.1/宿主名有效，且不影响常规解析）；
  2. 回归用例**先由容器解析一次、再把 IP 传给 naabu**（不在测试里赌 DNS 抖动）。
- **仍未根治**：`-sr` 兜底后对 Docker 特殊名仍可能抖动一次即失败（naabu
  不重试）。若将来生产确需宿主名目标，应在上层（工具调用前）做解析与重试，
  而不是改工具参数。

### R-19 fscan 默认在工作目录写 `result.txt`（观察）

- **发现时间**：2026-09-21（R-15 第三轮，测试跑完发现仓库根多了 `result.txt`）
- **现象**：`fscan` 无 `-o` 时仍把扫描结果写进 **cwd 的 `result.txt`**
  （实测内容为"开放端口/服务信息/Apache 与 CV 信息"等分节文本）。
- **影响**：容器执行时 cwd 由 `spec.workdir` 决定（未设则宿主进程 cwd）——
  即生产运行时结果文件会落在 agent 的工作目录；测试里则污染仓库根。
- **处置**：回归用例改用 `tmp_path` 作 `workdir`（`replace(spec,
  workdir=str(tmp_path))`），不再污染仓库。
- **待评估**：是否需要给 `fscan_scan` 固定一个工作目录（如 scratch 目录），
  或显式 `-o` 到受控位置——当前不改（属配置口径选择，先登记）。

### R-20 技能写错记忆分区 = 静默失效（已加防线 2026-09-21）

- **发现时间**：2026-09-21（做"技能种子"时，第一次实现就踩中）
- **现象**：`Memory(root)` 把技能写到 `root/skills/<默认分区>/`，而
  `PenAgent` 读的是 `memory.for_namespace(mode.memory_namespace)` 返回的
  **另一个目录**（`root/skills/<模式分区>/`，`penagent/agent.py:218`）。
  往前者种技能，agent 看不到——**不报错、不告警，分数一动不动**。
- **为什么危险**：这类失效没有症状。只看"跑了、没崩、有输出"会以为技能
  生效了，实际提示词里一个字都没有。
- **已加防线**（`tests/test_skill_seeds.py`）：
  - 正向：种子写进 `agent.memory`（agent 实际读取的对象）后，必须能通过
    `find_skills`（指纹关键词共现）与 `_filter_mode_skills`（模式技能包
    类别白名单）两道门，并出现在 `_system_prompt` 里；
  - 反向：故意种到默认分区，断言 `agent.memory.find_skills(...) == []`
    ——防止后人"修"回错误位置；
  - 入口：`python -m penagent skills --seed --mode <id>` 按模式解析分区，
    评测侧 `--seed-skills` 直接种到 `agent.memory`。
- **一般教训**：凡"写进去就该生效"的东西（技能 / 记忆 / 策略表），
  判据要落在**消费方视角**（agent 读到的那个对象），而不是写入方视角。

### R-21 技能注入上限 3 条：第 4 条种子会静默失效（已加防线 2026-09-21）

- **发现时间**：2026-09-21（扩充预置技能时核对注入链路）
- **现象**：`PenAgent.run` 注入的是 `skills[-3:]`——按成功率排序后取**最后
  3 条**（`penagent/agent.py`）。技能库里超过 3 条时，排在前面的被静默丢弃：
  不报错、不进提示词，只有 `injected_skills` 里看不到。
- **为什么危险**：与 R-20 同类——"知识写进去了但没生效"，从运行结果上
  看不出来。复现路径很自然：往 `skill_seeds.py` 加第 4 条种子即可。
- **已加防线**（`tests/test_skill_seeds.py`）：
  `test_all_seeds_survive_injection_top3_limit` —— 断言所有种子都在注入
  集合内，并断言种子数 ≤ 3。种子加到第 4 条时用例直接失败，逼一次显式取舍
  （合并技能 / 改注入上限），而不是让某条知识悄悄失效。
- **未改内核**：注入条数上限属既有设计（提示词长度与排序策略的取舍），
  本轮不动它——扩展技能时按"合并同类项"处理，确需更多容量时另开条目评估。



### R-22 宿主桥审计是全局的，未按 session → preset 过滤（**已修** 2026-09-22）

**位置**：`dsh/proteus-bridge/index.mjs`（host 平面 bundle 订阅 `session/event`）

**现象**：同一个 DSH 进程里**所有会话**的工具调用都会进链。实测链上混有非
Proteus 会话的 `read` / `grep` / `agent_teams_*` 调用。

**为什么是问题**：证据链是"结论可机验"的载体，也是 `--suite dsh-session` 的
判分输入。混入他会话调用后，"这条链属于哪次 Proteus 任务"失去依据——判分函数
（`score_from_evidence`）读到的是**整个进程**的动作合集。

**建议**：插件补一层 session → preset 映射，按 preset 归属过滤后再入链；或在链
记录里带上 preset 标识，由消费端（导入器 / 评分卡）过滤。

**来源**：`docs/DSH插件化与内核旁路治理.md` 第二节末的"已知边界"——该文承诺
"记进待办，不在本轮"，本条即兑现那次承诺。

**修法（2026-09-22）**：判据是 `SessionHeader.agentPreset`
（`packages/core/session/src/types.ts:93`，源码核实）。

- **插件侧**：每条审计记录带 `preset` 归属；另可配 `presets: ['proteus']`
  直接不记别的会话（缺省空 = 全记但打标）。
- **导入器侧**：`preset` 随 observation 与 tool_call 一起入链。
- **消费端**：`--suite dsh-session` 默认只算 `preset=proteus` 的记录
  （`--preset ''` 关闭过滤）。
- **关键取舍**：**归属未知（`''`）不过滤**。字段是新加的，老链没有它；形状变化
  时也可能取不到。按未知保留，比按未知丢弃安全——丢数据是静默的，多留是可查的。
- **裁决记录的归属是尽力而为**：`tools/pre-execute` 的载荷里没有 session，
  只能顺着 `exec.agent` 摸，取不到就是 `''`；权威归属来自 `session/event` 那条路。

### R-23 目标动作裁决的两条边界（观察，2026-09-22）

**位置**：`dsh/.agent-presets/proteus/proteus-tools-policy.mjs`

1. **只管 shell 工具**（`shellTools: [pwsh, bash]`）：模型改用其它能发网络请求的
   宿主工具（如 `web_fetch`）时本层不介入。实测那次由 DSH 自身 web provider 拒绝
   （`WEB_BLOCKED_URL`，provider 内建 SSRF 防护）——属另一道闸，**不是本层的覆盖**。
2. **目标识别是启发式**（URL / `--host` / `-u` / 裸 IP）：命令里做变量拼接或编码
   绕过时可能认不出。定位是"提高旁路成本 + 留痕"，**不是密不透风的 egress 管控**
   （DSH 没有 egress 官方词汇，能力边界见该设计文档第二节）。

**附带的档位语义**：`ask` 在**无审批应答者的 profile**（headless）里等价于拒绝
（fail-closed，实测 `requires approval, but …`）；web 会话里才是弹审批卡片。
默认档位在不同入口的实际效果不同，别把"默认 `ask`"当成宽松——与本清单 R-12 同类。

### R-24 安装副本漂移：改了的代码根本没上线（**已加防线** 2026-09-22）

**发现过程**：本轮去"把审计桥改成官方安装路径"时顺手核对，发现 `$DSH_HOME` 里
两份都是**陈旧副本**：

| 安装副本 | 仓库当前 | 后果 |
|---|---|---|
| `preset/proteus-tools-policy.mjs` 10950 字节（09-22 09:54） | 14885 字节（09-22 18:13） | 当天下午写的**内核缺位守卫根本没上线** |
| `node_modules/dsh-proteus-bridge/index.mjs` 5936 字节（09-21 23:10） | 1432 字节（09-22 09:54，已重构为薄再导出） | 审计层跑了两天前的代码 |

**为什么危险**：没有任何提示。会话照常起得来、工具照常能用，只是行为停在旧版
——与 R-20/R-21 同族（"写进去了但没生效"），但这次跨了宿主/内核边界。

**根因（两条，都靠真机才看清）**：

1. **preset 目录不能用链接**：第一版安装器把 `$DSH_HOME/.agent-presets/proteus`
   建成 junction，结果 DSH 的发现机制**不跟随 reparse point**——
   `discoverPresets` 返回数 **2 → 1**，proteus 直接从选择器里消失（无提示）。
   所以只能是真实目录 + 同步。
2. **官方安装路径此前真的坏着**：`dsh plugin add` 报
   `ERR_PNPM_UNEXPECTED_STORE`。根因是 **pnpm 11 不再从项目 `.npmrc` 读
   `store-dir`**（`pnpm config get store-dir` 返回 `undefined`），它必须写在
   `pnpm-workspace.yaml` 的 `storeDir`（pnpm 10+ 的设置新家）——profile 里其它
   设置都迁过去了，唯独漏了它。`.plugin-manager/logs/operation-vRZQu2/pnpm.log`
   里还留着 09-21 那次失败的原始记录，解释了当时为什么"只能手工登记"。

**已加防线**（`tools/dsh_install.py` + `dsh/start-proteus.cmd`）：
逐文件哈希比对（漂移即报并给同步命令）、启动前自动同步、`--check` 校验
`node_modules/dsh-proteus-bridge` 必须是链接（普通目录 = 旧副本）、备份放在
preset 根之外（放根里会被当成一个 preset）。18 条回归用例覆盖。

**顺带修好**：审计桥已按官方路径重装（`dsh plugin add`），
`node_modules/dsh-proteus-bridge` 现为 junction、与仓库同源。

### R-12 `ask` 档在无头 CLI 无人应答（F-E2E-5，观察）

- **证据**：`docs/端到端实战演练.md` 第一跑 `outcome=failed`——4 次工具调用全被 `default: ask` 拦截
- **现状**：靠 `auto_approve` 名单化解；Web 控制台的人工确认通道**尚未实测**
- **关联**：DSH 路径下由 DSH approval 承担，但 DSH preset 启动即带 `--authorize`（见 R-13）

### R-13 DSH preset 启动即带 `--authorize`，内层 ask 档失效（观察，属设计选择）

- **位置**：`dsh/.agent-presets/proteus/agent.cordis.yml` 的 `mcp-proteus` 行
  `args: ['-m','penagent','mcp','--data','data','--targets','127.0.0.1,localhost','--authorize']`
- **效果**：`--authorize` 是操作员级、服务端启动时一次设定 → 内核 `ask` 档在 DSH 会话内**不再拦截内层工具**，实际把关只剩 DSH 的 `approval` 档位
- **叠加风险**：`failOnStartupError: false` → 内核起不来时**静默丢掉全部工具**，不报错
  > **2026-09-22 补记**：这条已加机制性防线——`proteus-tools-policy.mjs` 的
  > **内核缺位守卫**在"工具面里没有 `mcp__proteus__*`"时对目标动作 fail-closed
  > （缺省 `kernelGuard: deny`，理由带排查命令），并把 `kernel` 标记写进证据链，
  > 链上因此能区分"被闸门拦下"与"内核根本没起来"。`--authorize` 那条仍是未决的
  > 设计选择（本节其余内容不变）。
- **建议**：文档明确写出"DSH 路径下靠的是哪一道闸"；评估是否需要 preset 侧不带 `--authorize`

---

## 五、已评审收口（仅登记，勿重复处理）

### D-1 `F-E2E-3` rsactf_attack 维持宿主直跑（已评审收口 2026-09-19）

- **结论**：离线数学攻击 CLI、无网络出口、无 shell 注入面；~~沙箱镜像内无 RsaCtfTool，提权等于废掉该工具~~（**2026-09-21 更新**：R-15 的专用镜像已内置 RsaCtfTool，该依据不再字面成立；但默认镜像不含它且需显式配置 `PENTEST_DOCKER_IMAGE`，Docker-free 环境提权后仍不可用——**结论维持宿主直跑**，理由改为"Docker-free 可用性"）
- **锁定**：`penagent/ctf_tools.json` 的 `review` 字段 + 契约用例 `tests/test_ctf_solve.py::test_rsactf_attack_review_locks_host_direct_tier`
- **再评审条件**：若改档，须连同契约用例与 `docs/端到端实战演练.md` 一起重新评审

### F-E2E-1 / F-E2E-2（已修，见 `docs/端到端实战演练.md:第四节`）

- F-E2E-1：`auto_approve` 工具名与内核注册名不一致 → 已修
- F-E2E-2：`eval_ctf_solve.py` 对 Docker 的隐性依赖 → 已改用免隔离 codec 工具链

---

## 六、统计

| 状态 | 条目 |
|---|---|
| 已修 | R-1, R-2（标注）, R-3, R-4, R-5, R-6, R-7, R-8, R-9, R-11（内核侧，含 2026-09-21 复核的路径缺陷）, R-14, **R-16**（2026-09-21 用户拍板选①：pentest-standard 改 `network_egress: true`，边界交 target_allowlist）, **R-17**（dalfox 调用形态修至 v3.2.0） |
| 已修 | **R-15**（专用镜像 + 命令容器化 + 字段解析补齐；四轮扩容后 RsaCtfTool / sqlmap / fscan / naabu / dalfox / nuclei / ffuf / gobuster / pocsuite3 全部进镜像，需模板与字典的那些改走**宿主目录只读挂载 + 按根重写**，见该条第 4 轮。**容器真跑待 Docker 恢复后执行**） |
| 已缓解 | **R-18**（naabu 解析 Docker 特殊名：`-sr` 兜底 + 测试传 IP；工具侧仍不重试） |
| 已修（防线） | **R-20**（技能写错记忆分区 = 静默失效；已加正反向用例与两个入口）、**R-21**（技能注入上限 3 条：第 4 条种子会静默失效；用例逼一次显式取舍）、**R-22**（审计链无 preset 归属 → 判分口径被稀释；已打标 + 消费端过滤 + 未知不丢）、**R-24**（安装副本漂移 = 改了的代码没上线；已加同步+哈希校验+链接校验） |
| 观察（基础设施/设计） | R-10, R-12, R-13, **R-19**（fscan 默认在 cwd 写 result.txt）、**R-23**（裁决层边界：只管 shell 工具 + 目标识别是启发式） |
| 已评审收口 | D-1, F-E2E-1, F-E2E-2 |

**R-11 的现状**：内核侧与容器侧都已闭环（错误提示 + Windows 路径缺陷修复），
基础设施（Docker 守护进程）当前在线但 ADR 记录它会闪断——环境侧仍不可控。
**R-15 已于 2026-09-23 第 4 轮闭环**（四个工具进镜像 + 宿主数据只读挂载）；
唯一的待办是"容器内真跑"这一步，卡在 Docker 暂停（环境），不是代码。

**最高优先级（2026-09-22 更新）**：**R-1 已修**（`eccd212`），"保护反成负担"那个恶性循环已断——
不传 `mode` 不再等于无保护。当前未决项里最该动的两条：

- **R-13 的另一半**：preset 的 `--authorize` 让内核 `ask` 档在 DSH 里失效——是继续
  这样（只靠宿主审批 + pre-execute 裁决），还是收回去让两道闸都在，需要拍板；
- **R-23**：裁决层只管 shell 工具、目标识别是启发式（提高旁路成本 + 留痕，不是
  密不透风的 egress 管控）。
