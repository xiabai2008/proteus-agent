name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest]
        python-version: ["3.12", "3.13"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run tests
        # torch 为可选依赖：RL 进化链路用例自动跳过，其余全量必须通过
        run: python -m pytest tests -q -rs
      - name: Capability gate (offline CTF suite)
        # 机制测试全绿 != 能力没退化：ctf 套件 33 题纯离线（约 5 秒），
        # 任一用例失败 benchmark.py 即以非零退出（docs/评测骨架.md）
        run: python examples/benchmark.py --suite ctf

  test-linux:
    # Linux 为实验性信号（项目以 Windows 为主），不阻塞合并
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run tests
        run: python -m pytest tests -q -rs
