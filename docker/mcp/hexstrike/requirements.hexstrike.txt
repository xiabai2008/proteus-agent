# HexStrike 精简容器依赖（2026-09-23）。
# 基于上游 requirements.txt 收窄：
#   - 去掉 pwntools / angr / bcrypt：实测非运行时依赖（只在生成的模板字符串里出现）
#   - 去掉 fastmcp 包：上游写错——适配器实际 `from mcp.server.fastmcp import FastMCP`，
#     依赖应为 mcp 本体，且钉 1.18.0（mcp 2.x 移除了相关装饰器 API 的兼容面，
#     与 docker/mcp 其他 server 同版本，2026-09-23 实测）
flask>=2.3.0,<4.0.0
requests>=2.31.0,<3.0.0
psutil>=5.9.0,<6.0.0
aiohttp>=3.8.0,<4.0.0
beautifulsoup4>=4.12.0,<5.0.0
selenium>=4.15.0,<5.0.0
webdriver-manager>=4.0.0,<5.0.0
mitmproxy>=9.0.0,<11.0.0
mcp==1.18.0
